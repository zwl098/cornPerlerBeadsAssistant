# ==================================================
# Git Commit Message 规范
# ==================================================
# 本规则适用于：
# - Git 面板中的「Generate with AI（✨）」
# - Copilot / Cursor / JetBrains AI 等自动生成提交信息功能
#
# 输出要求：
# 仅输出最终 Commit Message，不要包含解释、代码块、Markdown 或额外文本。
# ==================================================

# ==================================================
# 1. 格式规范
# ==================================================
#
# 优先格式：
# <type>(<scope>): <中文描述>
#
# scope 可选：
# <type>: <中文描述>
#
# 示例：
# feat(login): 新增短信验证码登录
# fix(user): 修复头像上传失败问题
# refactor(approval): 重构审批流状态管理
# chore(deps): 更新项目依赖版本
#

# ==================================================
# 2. 语言要求
# ==================================================
#
# - type 和 scope 必须使用英文小写
# - description 必须使用简体中文
# - 禁止英文描述（专业术语、库名、API 名称除外）
# - 使用动词开头：
#   新增、修复、优化、重构、更新、调整、移除、完善、支持
# - 不添加句号、感叹号等结尾标点
# - 建议控制在 50 个字符以内
# - 保持简洁、准确、可读
#

# ==================================================
# 3. Commit Type 定义
# ==================================================
#
# feat     新功能
# fix      Bug 修复
# docs     文档更新
# style    代码格式调整（不影响逻辑）
# refactor 代码重构（不新增功能、不修复 Bug）
# perf     性能优化
# test     测试相关
# build    构建系统、依赖、打包配置变更
# ci       CI/CD 配置变更
# chore    杂项维护、脚本、工具配置更新
#

# ==================================================
# 4. Scope 规范
# ==================================================
#
# 推荐使用以下模块名称（英文小写）：
#
# auth         认证
# login        登录
# user         用户管理
# role         角色权限
# approval     审批流程
# leave        请假
# attendance   考勤
# workflow     工作流
# form         表单
# table        表格
# dashboard    首页
# notification 通知消息
# upload       文件上传
# api          接口层
# router       路由
# store        状态管理
# h5           移动端
# admin        后台管理
# pwa          PWA
# i18n         国际化
# deps         依赖更新
# build        构建配置
# ci           持续集成
# docs         文档
#

# ==================================================
# 5. AI 生成规则（重要）
# ==================================================
#
# 生成 Commit Message 时必须遵守：
#
# 1. 优先使用：
#    <type>(<scope>): <中文描述>
#
# 2. 如果无法判断模块，可省略 scope：
#    <type>: <中文描述>
#
# 3. 不要默认使用 chore，
#    必须优先选择最符合语义的 type。
#
# 4. 多个改动同时存在时，
#    以主要变更内容作为 Commit 类型。
#
# 5. 合并以下情况：
#    - 新功能 + Bug 修复 → feat
#    - 重构 + Bug 修复 → fix
#    - 重构 + 性能优化 → perf
#    - 依赖升级 + 构建调整 → build
#
# 6. 不输出：
#    - Markdown
#    - 列表
#    - 解释说明
#    - 引号
#    - 代码块
#    - 前缀文字（例如："建议提交信息："）
#
# 7. 最终只输出一行合法的 Commit Message。
#

# ==================================================
# 6. 正确示例
# ==================================================
#
# feat(login): 新增短信验证码登录功能
# feat(h5): 支持移动端横屏展示
# fix(table): 修复横向滚动失效问题
# fix(api): 修复重复请求导致的数据异常
# refactor(store): 重构用户状态管理逻辑
# perf(list): 优化大数据列表渲染性能
# docs(readme): 补充本地开发说明
# build(vite): 升级构建配置并优化打包体积
# ci(github): 调整自动发布流程
# chore(deps): 更新项目依赖版本
#

# ==================================================
# 7. 错误示例
# ==================================================
#
# ❌ feat: add login feature
# ❌ 新增登录功能
# ❌ [feat] 新增登录功能
# ❌ feat(login): 新增登录功能。
# ❌ 建议提交信息：feat(login): 新增登录功能
# ❌ ```feat(login): 新增登录功能```
#
# ==================================================
