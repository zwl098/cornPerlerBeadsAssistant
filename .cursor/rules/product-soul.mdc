---
description: 拼豆助手产品灵魂与开发总控。任何改动都要服务「少数错一格」。
alwaysApply: true
---

# 拼豆助手 AI 开发规则

这是「拼豆图纸辅助工具」，不是图片编辑器、社区或 CAD。

核心目标：看得清 / 数得准 / 找得到。

永远优先：操作准、坐标准、高亮清、数格准、缩放流畅、移动端好用。

## 一句话

产品不是为了功能最多，而是让拼豆玩家**少数错一格**。

## 禁止

- 为加功能而加功能；擅自扩大需求
- 用大量 DOM 画格子（格子只存在于数字和像素）
- 破坏或另起一套坐标系统
- 让 UI 挡住图纸
- 一次性大改很多文件；为赶工留下明显技术债

## 坐标（硬规则）

一套 Camera，全家共用。管道固定：

`Screen(clientX/Y) − getBoundingClientRect → Canvas CSS → (canvas−offset)/scale → World → Grid`

主画布、网格、高亮、数格、完成、小地图必须走同一套 `coord` 函数。禁止 `offsetX`、禁止各算各的缩放。

新功能必须想清楚与 Zoom / Pan / Grid / Selection / Completed / History / Persistence 的关系。

## 技术

Vue 3 + TS + Composition API + Pinia + Canvas。状态进 Pinia，持久化进 IndexedDB。引擎纯 TS，Vue 只做壳。

## 产品

任何时候用户应能回答：我在哪？选中了什么？数到哪？完成了多少？

## 开发节奏

小步迭代。做完一个功能要说明：改了什么、影响范围、潜在问题、有没有破坏现有能力。

发现架构问题：先说明问题，再给方案，不要偷偷改坐标系。
